[project]
name = "helloworld_python_achieveravi"
version = "0.1.7"  # Bump the version number
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "achieveravi", email = "ravi.achieve@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
helloworld-python = "helloworld_python_achieveravi:main"

# FIX: Use 'modules' to explicitly list subdirectories for uv_build
[tool.uv.build-backend]
modules = [
    "helloworld_python_achieveravi",
    "helloworld_python_achieveravi.utils"
]
module-root = "src"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
